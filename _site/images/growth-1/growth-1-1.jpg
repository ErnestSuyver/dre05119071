from time import strftime
timestr = strftime('%Y%m%d-%H%M%S')

newPage()

# page 5

fill(0.45,0.20,0.19,0.8)
rect(0,0,width(),height())

for i in range(100):
    shadow((0, 0), 10, (0.1, 0, 0, 0.8))
    fill(0.3,0.2,0.7,0.7)
    oval(width() * random(), height() * random(),12,1.5)


w = 100
h = w

x = width() / 2 - w / 2
y = height() / 2 - h / 2

for j in range(100):
    stroke(0.9, 0.8, 0.9, 0.8)
    strokeWidth(12)
    fill(0.25,0.3,0.6,0.6)
    shadow((0, 0), 16, (0.8, 0.9, 0.75, 0.9))
    oval(x+200,y+175,w - j * 4,h - j * 1.7)
    rotate(0.82)


saveImage('growth-1-' + timestr + '.jpg')
