newPage()
# page 1

w = 1000
h = 1000
rows = 1
cols = rows # make as many columns as rows, to make a grid

fill(0,1,1)
strokeWidth(2)
stroke(0,0,0)

# this illustrates the basic grid setup *with a single dot*
for currentCol in range(cols):
    # the dot needs to be one third of the canvas size, so we can divide the canvas by number of dots (1) plus 2
    dotSize = w / (cols + 2)
    # in this simple grid, we just want to move the dot over by its own width (1/3 of the canvas)
    x = dotSize
    for currentRow in range(rows):
        # in this simple grid, we just want to move the dot up by its own height (1/3 of the canvas)
        y = dotSize
        # we want the dot to have equal-sized spacing on the side and bottom, so we can use the dotSize for the x and y coordinates, in addition to width and height
        rect(x, y, dotSize, dotSize)

newPage()
# page 2

w = 1000
h = 1000
rows = 2
cols = rows # make as many columns as rows, to make a grid

fill(0,1,1)
strokeWidth(2)
stroke(0,0,0)
        
# We want to position dots in a grid, so we first need to make a loop to change the x position for each new dot — this will create multiple columns.
for currentCol in range(cols):
    dotSize = w / (cols + 2)
    # we want to move the x position *each time the loop runs*, so we need to multiply the currentCol 
    x = w / (cols + 2) + currentCol * dotSize
    # We need a second loop to change the y position of dots, to make rows.
    for currentRow in range(rows):
        y = w / (rows + 2) + currentRow * dotSize

        rect(x, y, dotSize, dotSize)

newPage()
# page 3

w = 1000
h = 1000
rows = 2
cols = rows # make as many columns as rows, to make a grid

fill(0,1,1)
strokeWidth(2)
stroke(0,0,0)
  
# We want to position dots in a grid, so we first need to make a loop to change the x position for each new dot — this will create multiple columns.
for currentCol in range(cols):
    dotSize = w / (cols + (cols + 1))
    # we want to move the x position *each time the loop runs*, and add some extra space between each column
    x = dotSize + currentCol * (dotSize + dotSize)
    # We need a second loop to change the y position of dots, to make rows.
    for currentRow in range(rows):
        y = dotSize + currentRow * (dotSize + dotSize)
        
        rect(x, y, dotSize, dotSize)

newPage()
# page 4

w = 1000
h = 1000
rows = 8
cols = rows # make as many columns as rows, to make a grid

fill(0,1,1)
strokeWidth(2)
stroke(0,0,0)
  
# We want to position dots in a grid, so we first need to make a loop to change the x position for each new dot — this will create multiple columns.
for currentCol in range(cols):
    dotSize = w / (cols + (cols + 1))
    # we want to move the x position *each time the loop runs*, and add some extra space between each column
    x = dotSize + currentCol * (dotSize + dotSize)
    # We need a second loop to change the y position of dots, to make rows.
    for currentRow in range(rows):
        y = dotSize + currentRow * (dotSize + dotSize)
        
        rect(x, y, dotSize, dotSize)

newPage()
# page 5

w = 1000
h = 1000
rows = 18
cols = 6

fill(0,1,1)
strokeWidth(2)
stroke(0,0,0)
  
for currentCol in range(cols):
    dotWidth = w / (cols + (cols + 1))
    x = dotWidth + currentCol * (dotWidth + dotWidth)
    for currentRow in range(rows):
        dotHeight = w / (rows + (rows + 1))
        y = dotHeight + currentRow * (dotHeight + dotHeight)
        
        rect(x, y, dotWidth, dotHeight)

#The downside of this solution is that the dots don't have an independently definable space around them.
#I will fix that :)

# No,it is more subtle than that. This is drawing a grid,but only SOME POSITIONS ON THE GRID GET A SQUARE

#if you want to do it properly, you could give the grid an offset (is this even possible?)
#and then position the dots within each position in the grid (basically, their own little offset)


newPage()
# page 6

w = 1000
h = 1000
rows = 4
cols = 2

# fill(0,1,1)
# strokeWidth(2)
# stroke(0,0,0)
  
for currentCol in range(cols):
    dotWidth = w / (cols + (cols + 1))
    print("dotWidth is", dotWidth)
    x = dotWidth + currentCol * (dotWidth + dotWidth)
    print("x is", x)
    for currentRow in range(rows):
        dotHeight = w / (rows + (rows + 1))
        print("dotHeight is", dotHeight)
        y = dotHeight + currentRow * (dotHeight + dotHeight)
        print("y is", y)
        
        rect(x, y, dotWidth, dotHeight)

newPage()
# page 7

w = 1000
h = 1000
rows = 4
cols = 2

# fill(0,1,1)
# strokeWidth(2)
# stroke(0,0,0)
  
for currentCol in range(cols):
    dotWidth = w / (cols + (cols + 1))
    print("dotWidth is", dotWidth)
    x = dotWidth + currentCol * (dotWidth + dotWidth)
    print("x is", x)
    for currentRow in range(rows):
        dotHeight = w / (rows + (rows + 1))
        print("dotHeight is", dotHeight)
        y = dotHeight + currentRow * (dotHeight + dotHeight)
        print("y is", y)
        
        rect(x, y, dotWidth, dotHeight)

rows = 2
cols = 4

# fill(0,1,1)
# strokeWidth(2)
# stroke(0,0,0)
  
for currentCol in range(cols):
    dotWidth = w / (cols + (cols + 1))
    print("dotWidth is", dotWidth)
    x = dotWidth + currentCol * (dotWidth + dotWidth)
    print("x is", x)
    for currentRow in range(rows):
        dotHeight = w / (rows + (rows + 1))
        print("dotHeight is", dotHeight)
        y = dotHeight + currentRow * (dotHeight + dotHeight)
        print("y is", y)
        
        rect(x, y, dotWidth, dotHeight)

newPage()
# page 8

w = 1000
h = 1000
rows = 8
cols = 4

# fill(0,1,1)
# strokeWidth(2)
# stroke(0,0,0)
  
for currentCol in range(cols):
    dotWidth = w / (cols + (cols + 1))
    print("dotWidth is", dotWidth)
    x = dotWidth + currentCol * (dotWidth + dotWidth)
    print("x is", x)
    for currentRow in range(rows):
        dotHeight = w / (rows + (rows + 1))
        print("dotHeight is", dotHeight)
        y = dotHeight + currentRow * (dotHeight + dotHeight)
        print("y is", y)
        
        rect(x, y, dotWidth, dotHeight)

rows = 4
cols = 8

# fill(0,1,1)
# strokeWidth(2)
# stroke(0,0,0)
  
for currentCol in range(cols):
    dotWidth = w / (cols + (cols + 1))
    print("dotWidth is", dotWidth)
    x = dotWidth + currentCol * (dotWidth + dotWidth)
    print("x is", x)
    for currentRow in range(rows):
        dotHeight = w / (rows + (rows + 1))
        print("dotHeight is", dotHeight)
        y = dotHeight + currentRow * (dotHeight + dotHeight)
        print("y is", y)
        
        rect(x, y, dotWidth, dotHeight)

newPage()
# page 9

w = 1000
h = 1000
rows = 5
cols = 3

# fill(0,1,1)
# strokeWidth(2)
# stroke(0,0,0)
  
for currentCol in range(cols):
    dotWidth = w / (cols + (cols + 1))
    print("dotWidth is", dotWidth)
    x = dotWidth + currentCol * (dotWidth + dotWidth)
    print("x is", x)
    for currentRow in range(rows):
        dotHeight = w / (rows + (rows + 1))
        print("dotHeight is", dotHeight)
        y = dotHeight + currentRow * (dotHeight + dotHeight)
        print("y is", y)
        
        rect(x, y, dotWidth, dotHeight)

rows = 4
cols = 8

# fill(0,1,1)
# strokeWidth(2)
# stroke(0,0,0)
  
for currentCol in range(cols):
    dotWidth = w / (cols + (cols + 1))
    print("dotWidth is", dotWidth)
    x = dotWidth + currentCol * (dotWidth + dotWidth)
    print("x is", x)
    for currentRow in range(rows):
        dotHeight = w / (rows + (rows + 1))
        print("dotHeight is", dotHeight)
        y = dotHeight + currentRow * (dotHeight + dotHeight)
        print("y is", y)
        
        rect(x, y, dotWidth, dotHeight)

newPage()
# page 10

w = 1000
h = 1000
rows = 5
cols = 3

# fill(0,1,1)
# strokeWidth(2)
# stroke(0,0,0)
  
for currentCol in range(cols):
    dotWidth = w / (cols + (cols + 1))
    print("dotWidth is", dotWidth)
    x = dotWidth + currentCol * (dotWidth + dotWidth)
    print("x is", x)
    for currentRow in range(rows):
        dotHeight = w / (rows + (rows + 1))
        print("dotHeight is", dotHeight)
        y = dotHeight + currentRow * (dotHeight + dotHeight)
        print("y is", y)
        
        rect(x, y, dotWidth, dotHeight)

rows = 3
cols = 17

# fill(0,1,1)
# strokeWidth(2)
# stroke(0,0,0)
  
for currentCol in range(cols):
    dotWidth = w / (cols + (cols + 1))
    print("dotWidth is", dotWidth)
    x = dotWidth + currentCol * (dotWidth + dotWidth)
    print("x is", x)
    for currentRow in range(rows):
        dotHeight = w / (rows + (rows + 1))
        print("dotHeight is", dotHeight)
        y = dotHeight + currentRow * (dotHeight + dotHeight)
        print("y is", y)
        
        rect(x, y, dotWidth, dotHeight)

newPage()
# page 11

w = 1000
h = 1000
rows = 31
cols = 3

# fill(0,1,1)
# strokeWidth(2)
# stroke(0,0,0)
  
for currentCol in range(cols):
    dotWidth = w / (cols + (cols + 1))
    print("dotWidth is", dotWidth)
    x = dotWidth + currentCol * (dotWidth + dotWidth)
    print("x is", x)
    for currentRow in range(rows):
        dotHeight = w / (rows + (rows + 1))
        print("dotHeight is", dotHeight)
        y = dotHeight + currentRow * (dotHeight + dotHeight)
        print("y is", y)
        
        rect(x, y, dotWidth, dotHeight)

rows = 3
cols = 17

# fill(0,1,1)
# strokeWidth(2)
# stroke(0,0,0)
  
for currentCol in range(cols):
    dotWidth = w / (cols + (cols + 1))
    print("dotWidth is", dotWidth)
    x = dotWidth + currentCol * (dotWidth + dotWidth)
    print("x is", x)
    for currentRow in range(rows):
        dotHeight = w / (rows + (rows + 1))
        print("dotHeight is", dotHeight)
        y = dotHeight + currentRow * (dotHeight + dotHeight)
        print("y is", y)
        
        rect(x, y, dotWidth, dotHeight)
