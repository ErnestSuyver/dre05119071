newPage()
# page 1

grid = 100
for y in range(0, height(), grid):
    for x in range(0, width(), grid):
        fill(1,150/255,0)
        stroke(1,0,0)
        strokeWidth(10)
        oval(x,y,100,100)
saveImage("grid-2-1.jpeg")       

newPage()
# page 2

grid = 100
for y in range(0, height(), grid):
    for x in range(0, width(), grid):
        fill(1,150/255,0)
        stroke(1,0,0)
        strokeWidth(10)
        oval(x+5,y+5,100,100)
saveImage("grid-2-2.jpeg")       
        
newPage()
#page 3

grid = 100
for y in range(0, height(), grid):
    for x in range(0, width(), grid):
        fill(random(),random(),random())
        stroke(1,0,0)
        strokeWidth(10)
        oval(x,y,100,100)


## pages 4 t/m 13
  
for page in range(10):
    grid = 100
    newPage(1000,1000)
    for y in range(0, height(), grid):
        for x in range(0, width(), grid):
            fill(random(),random(),random())
            oval(x,y,100,100)

saveImage("grid-2-4-13.gif")       

newPage()
# Page 14

grid = 100
for y in range(0, height(), grid):
    for x in range(0, width(), grid):
        fill(random(),random(),random())
        stroke(1,0,0)
        strokeWidth(10)
        oval(x,y,250,250)

grid = 100
for y in range(0, height(), grid):
    for x in range(0, width(), grid):
        fill(random(),random(),random())
        stroke(1,0,0)
        strokeWidth(10)
        oval(x,y,100,100)

saveImage("grid-2-14.jpeg")       

newPage()
# Page 15

grid = 100
for y in range(0, height(), grid):
    for x in range(0, width(), grid):
        fill(random(),random(),random())
        # stroke(1,0,0)
        # strokeWidth(10)
        oval(x,y,250,250)

grid = 100
for y in range(0, height(), grid):
    for x in range(0, width(), grid):
        fill(random(),random(),random())
        # stroke(1,0,0)
        # strokeWidth(10)
        oval(x,y,100,100)

saveImage("grid-2-15.jpeg")       

newPage()
# Page 16

grid = 100
for y in range(0, height(), grid):
    for x in range(0, width(), grid):
        fill(random(),random(),random())
        # stroke(1,0,0)
        # strokeWidth(10)
        oval(x,y,250,250)

grid = 100
for y in range(0, height(), grid):
    for x in range(0, width(), grid):
        fill(random(),random(),random())
        # stroke(1,0,0)
        # strokeWidth(10)
        oval(x,y,100,100)

grid = 100
for y in range(0, height(), grid):
    for x in range(0, width(), grid):
        fill(random(),random(),random())
        # stroke(1,0,0)
        # strokeWidth(10)
        oval(x,y,35,35)

saveImage("grid-2-16.jpeg")       

newPage()
# Page 17

fill(0.3,0.2,0,0.9)
rect(0,0,width(),height())

grid = 100
for y in range(0, height(), grid):
    for x in range(0, width(), grid):
        fill(random(),random(),random())
        # stroke(1,0,0)
        # strokeWidth(10)
        oval(x,y,250,250)

grid = 100
for y in range(0, height(), grid):
    for x in range(0, width(), grid):
        fill(random(),random(),random())
        # stroke(1,0,0)
        # strokeWidth(10)
        oval(x+20,y+20,100,100)

grid = 100
for y in range(0, height(), grid):
    for x in range(0, width(), grid):
        fill(random(),random(),random())
        # stroke(1,0,0)
        # strokeWidth(10)
        oval(x+30,y+30,35,35)

saveImage("grid-2-17.jpeg")       

