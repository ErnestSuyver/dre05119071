from time import strftime
timestr = strftime('%Y%m%d-%H%M%S')

newPage()

#fill(0.1,0.5,0.5)
fill(.8)
rect(0,0,width(),height())

for i in range(500):
    save()
    shadow((-5, -5), 10, (0.9, 0.9, 0.9, 0.85))
    fill(0.6,0.6,0.19,0.9)
    oval(width() * random(), height() * random(), 3,2)
    restore()

w = 30 #width

for i in range(100):
    #save()
    shadow((-15, -15), 10, (0.3, 0.25, 0.25, 0.90))
    stroke(0.5,0.4,0.4)
    strokeWidth(10 * w)
    fill(.89)
    oval(450,650,w,0.5 * w)
    rotate(13)
    #®restore()


saveImage('letter-o-attempt-1-1' + timestr + '.jpg')
