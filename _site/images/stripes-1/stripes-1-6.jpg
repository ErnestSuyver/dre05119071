#stripes-1

newPage(100,100)
# page1

fill(1)
rect(0, 0, width(), height())

for i in range(1,6,1):
    w = i ** 2
    translate(i + w)
    fill(0)
    rect(0, 0, w, height())
    # print("Dit is streepje", i, "met dikte", w, "beginnend bij x-je", w)

saveImage("stripes-1-1.jpeg")

newPage(100,100)
# page2

fill(1)
rect(0, 0, width(), height())

for i in range(1,6,1):
    w = i ** 2
    translate(10)
    fill(0)
    rect(0, 0, w, height())
    
saveImage("stripes-1-2.jpeg")

newPage(100,100)
# page3

fill(1)
rect(0, 0, width(), height())

for i in range(1,6,1):
    w = i ** 2
    translate(i)
    fill(0)
    rect(0, 0, w, height())

saveImage("stripes-1-3.jpeg")

newPage(100,100)
# page4

fill(1)
rect(0, 0, width(), height())

for i in range(1,6,1):
    w = i ** 2
    translate(w)
    fill(0)
    rect(0, 0, w, height())

saveImage("stripes-1-4.jpeg")

newPage(100,100)
# page5

fill(1)
rect(0, 0, width(), height())

for i in range(1,6,1):
    w = i ** 2
    translate(w * i)
    fill(0)
    rect(0, 0, w, height())

saveImage("stripes-1-5.jpeg")

saveImage("stripes-1.gif")

 