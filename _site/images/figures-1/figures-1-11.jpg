# figures 2
   
# page 1
w, h = 200, 200 # size of the pages / frames
newPage(w, h) # create a new page
fill(0.75)
rect(5,5,w-10,h-10) # set background

# loop over the amount of frames needed
for i in range(10):
    save()
    fill(0.85, 0.45, 0.15) # set fill color
    translate(w / 2, h / 2) # translate to the center of the page
    rotate(i*10) # rotate around the center ditwas10
    oval(-50, -50, 50, 50) # draw the rect
    restore()


#range36 makes a doughnut
#ranges15-25 make a worm
#range10 makes a kind of heart

# saveImage("figures-2-1.jpg")

# page 2
w, h = 200, 200 # size of the pages / frames
newPage(w, h) # create a new page
fill(0.75)
rect(5,5,w-10,h-10) # set background

# loop over the amount of frames needed
for i in range(10):
    save()
    fill(0.85, 0.45, 0.15) # set fill color
    translate(w / 2, h / 2) # translate to the center of the page
    rotate(i*36) # rotate around the center ditwas10
    oval(-50, -50, 50, 50) # draw the rect
    restore()


#increasing the iteration factor makes the curve smaller so you need less circles
#i36 for example again makes a doughnut,but now with only 10 iterations

saveImage("figures-2-2.jpg")

# page 3
w, h = 200, 200 # size of the pages / frames
newPage(w, h) # create a new page
fill(0.75)
rect(5,5,w-10,h-10) # set background

# loop over the amount of frames needed
for i in range(72):
    save()
    fill(0.85, 0.45, 0.15) # set fill color
    translate(w / 2, h / 2) # translate to the center of the page
    rotate(i*72) # rotate around the center ditwas10
    oval(-50, -50, 50, 50) # draw the rect
    restore()


#crazy stuff happens when BOTH range and iteration are high
#range72 and i72 for example
#or range12 and i90
#or range12 and i180

saveImage("figures-2-3.jpg")
