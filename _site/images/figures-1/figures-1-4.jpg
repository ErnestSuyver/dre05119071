# figures 1
   
# page 1
w, h = 200, 200 # size of the pages / frames
newPage(w, h) # create a new page
fill(0.75)
rect(5,5,w-10,h-10) # set background

# loop over the amount of frames needed
for i in range(10):
    save()
    fill(0.85, 0.45, 0.15) # set fill color
    translate(w / 2, h / 2) # translate to the center of the page
    rotate(i*10) # rotate around the center ditwas10
    rect(-50, -50, 50, 50) # draw the rect
    restore()

saveImage("figures-1-1.jpg")

# page 2
w, h = 200, 200 # size of the pages / frames
newPage(w, h) # create a new page
fill(0.75)
rect(5,5,w-10,h-10) # set background

# loop over the amount of frames needed
for i in range(10):
    save()
    fill(0.85, 0.45, 0.15) # set fill color
    translate(w / 2, h / 2) # translate to the center of the page
    rotate(i*36) # rotate around the center ditwas10
    rect(-50, -50, 50, 50) # draw the rect
    restore()

# between i*25 and i*30 you get apackman like figure
# i*36 = 11 fingered star

saveImage("figures-1-2.jpg")

# page 3
w, h = 200, 200 
newPage(w, h)
fill(0.75)
rect(5,5,w-10,h-10) 

for i in range(10):
    save()
    fill(0.85, 0.45, 0.15)
    translate(w / 2, h / 2) # translate to canvas center
    rotate(i*45)
    rect(-50, -50, 50, 50)
    restore()

# i*72 = 5 fingered star
# i*40 = 9 fingered star
# i*45 = 8 fingered star

saveImage("figures-1-3.jpg")

# page 4
w, h = 200, 200 
newPage(w, h)
fill(0.75)
rect(5,5,w-10,h-10) 

for i in range(36):
    save()
    fill(0.85, 0.45, 0.15)
    translate(w / 2, h / 2) # translate to canvas center
    rotate(i*10)
    rect(-50, -50, 50, 50)
    restore()

#set the range to 36 and you get this multi-pointed star
#idem for 72


#so:
#increase theiterator for more squares
#increase the rotation factor to change the angle of the squares

saveImage("figures-1-4.jpg")

# page 5
w, h = 200, 200 
newPage(w, h)
fill(0.75)
rect(5,5,w-10,h-10) 

for i in range(8):
    save()
    fill(0.85, 0.45, 0.15)
    translate(w / 2, h / 2) # translate to canvas center
    rotate(i*45)
    rect(-50, -50, 50, 50)
    restore()

#same result as on page 3 much more efficient, i.e. less iterations needed because of increased rotation angle

saveImage("figures-1-5.jpg")

# page 6
w, h = 200, 200 
newPage(w, h)
fill(0.75)
rect(5,5,w-10,h-10) 

for i in range(3):
    save()
    fill(0.85, 0.45, 0.15)
    translate(w / 2, h / 2) # translate to canvas center
    rotate(i*90)
    rect(-50, -50, 50, 50)
    restore()

#very interesting: range2 places a new square to the right, range3 on top of that, and range 4 completes the bigger square

saveImage("figures-1-6.jpg")

# page 7
w, h = 200, 200 
newPage(w, h)
fill(0.75)
rect(5,5,w-10,h-10) 

for i in range(5):
    save()
    fill(0.85, 0.45, 0.15)
    translate(w / 2, h / 2) # translate to canvas center
    rotate(i*135)
    rect(-50, -50, 50, 50)
    restore()

#higher angles create crazy stuff

saveImage("figures-1-7.jpg")

# page 8
w, h = 200, 200 
newPage(w, h)
fill(0.75)
rect(5,5,w-10,h-10) 

for i in range(2):
    save()
    fill(0.85, 0.45, 0.15)
    translate(w / 2, h / 2) # translate to canvas center
    rotate(i*180)
    rect(-50, -50, 50, 50)
    restore()

#nothing happens above range2

saveImage("figures-1-8.jpg")