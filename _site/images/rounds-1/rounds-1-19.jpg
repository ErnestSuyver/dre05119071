# newPage(500,500)

# #nr_frames = 3

# fill(0, 0.5, 1, 0.8)
# rect(0, 0, width(), height())

# ball_dia = width() / 8

# fill(0.1, 0.35, 0.9, 0.9)
# oval(width() / 2 - 0.5 * ball_dia, width() / 2 - 0.5 * ball_dia, 2 * ball_dia, 2 * ball_dia)

# # fill(0.1, 0.35, 0.6, 0.9)
# # oval(width() / 2 - 0.5 * ball_dia, width() / 2 - 0.5 * ball_dia, ball_dia, ball_dia)

# newPage(500,500)

# fill(0.1, 0.35, 0.9, 0.9)
# oval(width() / 2 - 0.5 * ball_dia, width() / 2 - 0.5 * ball_dia, 200, 200)

# newPage(500,500)

# fill(0.1, 0.35, 0.9, 0.9)
# oval(width() / 2, width() / 2, 200, 200)

# newPage(500,500)

# fill(0.1, 0.35, 0.9, 0.9)
# oval(width() / 2, width() / 2, 200, 200)

# newPage(500,500)

# fill(0.1, 0.35, 0.9, 0.9)
# oval(width() / 2 - 200 / 2, width() / 2 - 200 /2, 200, 200)

# newPage(500,500)
# o_dia = 200
# fill(0.1, 0.35, 0.9, 0.9)
# oval(width() / 2 - o_dia / 2, width() / 2 - o_dia / 2, o_dia, o_dia)

# o_dia = 200
# o_size = 2
# fill(0.1, 0.35, 0.9, 0.9)
# oval(width() / 2 - o_dia * o_size / 2, width() / 2 - o_dia * o_size / 2, o_dia * o_size, o_dia * o_size)


nr_frames = 5
for frame in range(nr_frames):
    # newPage(500, 500)
    # frameDuration(1/5)

    o_dia = 200
    o_size = 1 + frame #hoe zorgik ervoor dat hij binnen het canvas blijft?
    d = o_dia * o_size
    a = 0.9 * 0.1 * o_size #de wisselende transparency zorgt voor het kleurverschil
    fill(0.1, 0.35, 0.9, a)
    oval(width() / 2 - d / 2, width() / 2 - d / 2, d, d) #lekker opgeruimd door het d-tje

saveImage("balletje-1.jpeg")