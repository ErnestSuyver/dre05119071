newPage(500, 500)

for o in range(-5,0,1):

    o_dia = 100
    #o_size = 1 + 0.5 * o
    o_size = 1 + 0.5 * -1 * o
    d = o_dia * o_size
    o_fill = 0.9 + 0.019 * -1 * o
    fill(o_fill)
    oval(width() / 2 - d / 2, width() / 2 - d / 2, d, d)
    #print("Voor cirkel", o, "is de diameter", d, "en de transparantie is", o_fill)

saveImage("nog-een-bal-1.jpeg")

newPage(500, 500)

for o in range(-5,0,1):
    o_dia = 100
    o_size = abs(o)
    d = o_dia * o_size
    o_fill = 0.9 + 1 / o
    fill(o_fill)
    oval(width() / 2 - d / 2, width() / 2 - d / 2, d, d)
    #print("Voor cirkel", o, "is de diameter", d, "en de transparantie is", o_fill)

saveImage("nog-een-bal-2.jpeg")

newPage(500, 500)

nr_frames = 5

for frame in range(nr_frames):
    newPage(500, 500)
    frameDuration(1/5)
    
    o_dia = 100
    o_size = 1 + frame
    d = o_dia * o_size
    o_fill = 0.9 + 0.02 * frame
    fill(o_fill)
    oval(width() / 2 - d / 2, width() / 2 - d / 2, d, d)
    print(o_fill)

saveImage("nog-een-bal-3.gif")
