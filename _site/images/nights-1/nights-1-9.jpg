from time import strftime
timestr = strftime('%Y%m%d-%H%M%S')

newPage()

# page 1

fill(0.03,0.02,0.01,0.99)
rect(0,0,width(),height())

for i in range(1500):
    save()
    shadow((-5, -5), 10, (0.4, 0.35, 0.35, 0.85))
    fill(0.6,0.3,0.2,0.9)
    oval(width() * random(), height() * random(),30 + i / 10,1.5)
    restore()
    
#saveImage('noce-1-' + timestr + '.jpg')
   
newPage()

# page 2

fill(0.03,0.02,0.01,0.89)
rect(0,0,width(),height())

for i in range(1000):
    save()
    shadow((-5, -5), 10, (0.7, 0.7, 0.7, 0.85))
    fill(0.7,0.7,0.7,0.9)
    oval(width() * random(), height() * random(),3 + i / 10,1.5 * random())
    restore()

#saveImage('noce-2-' + timestr + '.jpg')

newPage()

# page 3

fill(0.15,0.09,0.09,0.89)
rect(0,0,width(),height())

for i in range(500):
    save()
    shadow((-5, -5), 10, (0.7, 0.7, 0.7, 0.85))
    fill(0.2,0.2,0.45,0.9)
    oval(width() * random(), height() * random(),3 + i / 10,1.5 * random())
    restore()

saveImage('noce-3-' + timestr + '.jpg')